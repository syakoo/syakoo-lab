pre-push:
  parallel: true
  commands:
    lint:
      run: pnpm lint
    test:
      run: pnpm run test --run

commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint -e
    emoji:
      run: pnpm exec devmoji -e
