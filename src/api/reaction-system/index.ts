import { getReaction } from "./get-reaction";
import { postReactionIncrement } from "./post-reaction-increment";
import { syncReaction } from "./sync-reaction";

export const api = {
  getReaction,
  postReactionIncrement,
  syncReaction,
};
