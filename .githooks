[hook "lint"]
	event = pre-push
	command = sh -c 'pnpm lint'

[hook "test"]
	event = pre-push
	command = sh -c 'pnpm run test --run'

[hook "commitlint"]
	event = commit-msg
	command = pnpm exec commitlint -e
